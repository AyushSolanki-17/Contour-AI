# Development-only PostgreSQL settings. Copy to .env and replace the password.
CONTOUR_POSTGRES_DB=contour_dev
CONTOUR_POSTGRES_USER=contour
CONTOUR_POSTGRES_PASSWORD=change-me-local-only
CONTOUR_POSTGRES_HOST=127.0.0.1
CONTOUR_POSTGRES_PORT=5432
CONTOUR_CURSOR_SIGNING_SECRET=change-me-local-only
